<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HttpsServerWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HttpsServerWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Local HttpsServer in WinUI"
>
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid ColumnDefinitions="*, *">
        <Grid RowDefinitions="auto, auto">
            <Button Content="Upload file from App" Click="{x:Bind UploadFileFromApp}" Grid.Row="0" />
            <Button Content="Upload file from WebView" Grid.Row="1" />
        </Grid>
        <Grid Grid.Column="1">
            <ListView SelectionMode="None" ItemsSource="{x:Bind Items}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{x:Bind}" />
                            <Button Content="Show in app" />
                            <Button Content="Show in WebView" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Window>
