<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="HttpsServerWinUI.Preview.ViewModels.PreviewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HttpsServerWinUI.Preview.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:webview="using:HttpsServerWinUI.WebView"
    xmlns:views="using:HttpsServerWinUI.Preview.Views"
    xmlns:preview="using:HttpsServerWinUI.Preview"
    mc:Ignorable="d"
>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- TOP -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical">
                <Button Content="Upload file from App" Command="{x:Bind ViewModel.UploadFileFromAppCommand}" Grid.Row="0" />
                <Button Content="Upload file from WebView" Command="{x:Bind ViewModel.UploadFileFromWebViewCommand}" Grid.Row="1" />
            </StackPanel>
            <Grid Grid.Column="1">
                <ListView SelectionMode="None" ItemsSource="{x:Bind ViewModel.Files}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="preview:FileInfo">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{x:Bind Id}" />
                                <Image Source="{x:Bind Path}" Width="200" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>

        <!-- BOTTOM -->
        <views:WebViewControl Grid.Row="1" />
    </Grid>
</UserControl>
